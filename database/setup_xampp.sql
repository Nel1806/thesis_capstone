-- Create MySQL database for Teacher Audit System (XAMPP / MariaDB)
-- Run in phpMyAdmin or: mysql -u root < database/setup_xampp.sql

CREATE DATABASE IF NOT EXISTS thesis_capstone
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
